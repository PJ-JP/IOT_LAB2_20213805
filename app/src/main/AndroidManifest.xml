<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">
        <activity
            android:name=".CrearSwitchActivity"
            android:exported="false" />
        <activity
            android:name=".entity.SwitchActivity"
            android:exported="false"
            android:label="Switches"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DetallesSwitchActivity"
            android:exported="false" />
        <activity
            android:name=".CrearRouterActivity"
            android:exported="false"
            android:label="Crear Router"
            android:parentActivityName=".RouterActivity" />
        <activity
            android:name=".DetallesRouterActivity"
            android:exported="false"
            android:label="Actualizar Router"
            android:parentActivityName=".RouterActivity" />
        <activity
            android:name=".RouterActivity"
            android:exported="false"
            android:label="Routers"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="AppIOT - Lab 2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>